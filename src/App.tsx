import ProductConfigurator from "./pages/ProductConfigurator";

function App() {
  // If we had any other routes, I'd handle them here, but
  // ignored that for the moment.
  return <ProductConfigurator />;
}

export default App;
